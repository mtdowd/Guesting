<%= render @bar, user: current_user %>

<% if current_user.manager? %>
  <%= render "manager", bar: @bar, user: current_user %>
<% else %>
  <%= render "bartender", bar: @bar, user: current_user %>
<% end %>
